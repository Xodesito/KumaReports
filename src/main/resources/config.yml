database:
  mongodb:
    enabled: true
    url: 'noSpecified'
discordBot:
  enabled: true
  token: 'noSpecified'
  guildId: 'noSpecified'
  # Valid status: ONLINE, IDLE, INVISIBLE, DO_NOT_DISTURB, OFFLINE
  status: 'ONLINE'
  activity:
    type: 'PLAYING'
    text: 'KumaReports!'
  # Set the channel ID to receive the reports
  reportsChannel: 'noSpecified'
  report:
    embed:
      footerIcon: 'noIcon'
      authorUrl: 'noUrl'
      authorIcon: 'noIcon'
      thumbnail: 'noThumbnail'
      imageUrl: 'noImageUrl'
      color: 'noColor'
    fields:
      reporter: true
      reported: true
      reason: true
      reportId: true
      date: true


